{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  {{- if .Permalink }}
  {{- $priority := "0.5" }}
  {{- $changefreq := "monthly" }}
  {{- if .IsHome }}
    {{- $priority = "1.0" }}
    {{- $changefreq = "weekly" }}
  {{- else if or (in .RelPermalink "iracing-overlay") (in .RelPermalink "lmu-overlay") }}
    {{- $priority = "0.9" }}
  {{- else if in .RelPermalink "-overlay" }}
    {{- $priority = "0.8" }}
  {{- else if or (in .RelPermalink "/features") (in .RelPermalink "/install") }}
    {{- $priority = "0.7" }}
  {{- else if in .RelPermalink "/changelog" }}
    {{- $priority = "0.6" }}
    {{- $changefreq = "weekly" }}
  {{- else if in .RelPermalink "/faq" }}
    {{- $priority = "0.6" }}
  {{- end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>
    {{- end }}
    <changefreq>{{ $changefreq }}</changefreq>
    <priority>{{ $priority }}</priority>
  </url>
  {{- end }}
  {{- end }}
</urlset>
